{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "howlforge.persona/v1",
  "title": "HowlForge Persona",
  "description": "An optional organizational identity bound to a role. A persona resolves to a role and stops there; it deliberately cannot name a provider, model or runtime, because a persona that could pin a model would die when that model did.",
  "type": "object",
  "properties": {
    "schema_version": { "type": "string", "const": "howlforge.persona/v1" },
    "id": { "type": "string", "pattern": "^[a-z0-9_-]{1,64}$" },
    "role": { "type": "string", "pattern": "^[a-z0-9_-]{1,64}$" },
    "display_name": { "type": "string" },
    "description": { "type": "string" },
    "style_notes": { "type": "array", "items": { "type": "string" } }
  },
  "required": ["schema_version", "id", "role"],
  "additionalProperties": false
}
